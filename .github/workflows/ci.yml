name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  lint-and-test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'yarn'
          cache-dependency-path: |
            frontend/nextjs/yarn.lock

      - name: Run setup script
        run: |
          if [ -f "nrsgirls-platform/scripts/setup.sh" ]; then
            bash nrsgirls-platform/scripts/setup.sh
          fi

      - name: Install frontend dependencies
        run: |
          if [ -d "frontend/nextjs" ] && [ -f "frontend/nextjs/package.json" ]; then
            cd frontend/nextjs
            yarn install --frozen-lockfile
          fi

      - name: Lint and test frontend
        run: |
          if [ -f "nrsgirls-platform/scripts/lint-test.sh" ]; then
            bash nrsgirls-platform/scripts/lint-test.sh
          fi

      - name: Build frontend
        run: |
          if [ -d "frontend/nextjs" ] && [ -f "frontend/nextjs/package.json" ]; then
            cd frontend/nextjs
            yarn build
          fi
